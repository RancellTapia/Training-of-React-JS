import React from 'react';
import { AppBar, Container, Typography } from '@material-ui/core';


const Nav = () => {
    return (
        <AppBar>
            <Container>
                <Typography variant = "h3">RD Trabaja</Typography>
            </Container> 
        </AppBar>
      );
}
 
export default Nav;